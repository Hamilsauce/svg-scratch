import Node from './Node.js';
import TextNode from './TextNode.js';

const _SVG_NS = 'http://www.w3.org/2000/svg';

const vertexType = {
  rect: 'rect',
  circle: 'circle',
}

// VERTEX
export default class extends Node {/c/
  constructor(pos, color, graph) {}
}